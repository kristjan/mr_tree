# ESPHome secrets for Mr Tree.
#
# You normally do NOT create this by hand: flash.sh runs gen_secrets.sh, which
# reads WIFI_SSID / WIFI_PASSWORD from ../settings.toml and writes secrets.yaml.
# This example just documents the keys.
wifi_ssid: "your-wifi-ssid"
wifi_password: "your-wifi-password"
